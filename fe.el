;;; fe.el --- Entry point for Fe major mode -*- lexical-binding: t; -*-

;;; Commentary:

;; Load `fe-mode' and provide the `fe' package feature for package managers
;; and user configs that require `fe'.

;;; Code:

(require 'fe-mode)

(provide 'fe)
;;; fe.el ends here
